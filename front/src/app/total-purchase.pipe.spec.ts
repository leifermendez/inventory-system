import { TotalPurchasePipe } from './total-purchase.pipe';

describe('TotalPurchasePipe', () => {
  it('create an instance', () => {
    const pipe = new TotalPurchasePipe();
    expect(pipe).toBeTruthy();
  });
});

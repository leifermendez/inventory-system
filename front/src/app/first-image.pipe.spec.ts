import { FirstImagePipe } from './first-image.pipe';

describe('FirstImagePipe', () => {
  it('create an instance', () => {
    const pipe = new FirstImagePipe();
    expect(pipe).toBeTruthy();
  });
});
